#include <bits/stdc++.h>
#define ll long long
#define Andy8787_want_AC ios::sync_with_stdio(0),cin.tie(0);cout.tie(0);
using namespace std;

int main() {
    Andy8787_want_AC
    ll n,x, ans = 0;
    vector<ll> a(3);
    cin >> n >> a[0] >> a[1] >> a[2] >> x;
    sort(a.begin(), a.end());
    for (ll i = 0; i < n; i++) {
        
    }
}