#include <iostream>
#include <bits/stdc++.h>
#include <cmath>
#include <map>
#include <string>
#include <algorithm>
#include <vector>
#define ll long long
using namespace std;

ll n,k,m;

int main() {
    ios::sync_with_stdio(0),cin.tie(0);
    cin >> n >> k >> m;
    ll u[n],c[n],t[n];
    for (int i = 0; i < n; i++) {
        cin >> u[i];
    }
    for (int i = 0; i < n; i++) {
        cin >> c[i] >> t[i];
    }
    cout << 1 << "\n";
}