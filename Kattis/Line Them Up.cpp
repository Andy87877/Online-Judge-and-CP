// https://open.kattis.com/problems/lineup
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, num;
    cin >> n;
    vector<string> vec(n), up, down;
    for (int i = 0; i < n; i++) {
        cin >> vec[i];
    }
    
}