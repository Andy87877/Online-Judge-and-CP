#include <bits/stdc++.h>
#define ll long long
#define Andy8787_want_AC ios::sync_with_stdio(0),cin.tie(0);cout.tie(0);
using namespace std;

ll judge(){
    ll n;
    cin >> n;
    ll a[n];
    for (ll i = 0; i < n; i++) {
        cin >> a[i];
    }

    
}

int main() {
    Andy8787_want_AC
    ll t;
    cin >> t;
    while(t--){
        cout << judge() << "\n";
    }
}