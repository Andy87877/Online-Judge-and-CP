#include <bits/stdc++.h>
#define ll long long
using namespace std;

string sovle() {
    string s, sus;
    cin >> s >> sus;

    char temp = s[0];
    vector<char> ch;
    vector<ll> time;
    for (ll i = 1; i < s.size(); i++) {
        if (temp != )
    }

}

int main() {
    ios::sync_with_stdio(0),cin.tie(0);
    cout << sovle() << "\n";
}